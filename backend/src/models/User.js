import bcrypt from 'bcryptjs';import mongoose from 'mongoose';
const userSchema=new mongoose.Schema({name:{type:String,required:true,index:true},email:{type:String,required:true,unique:true,lowercase:true,index:true},password:{type:String,required:true,select:false},role:{type:String,enum:['student','moderator','admin'],default:'student',index:true},avatar:String,bio:String,verified:{type:Boolean,default:false},refreshTokenHash:String,passwordResetToken:String,emailVerificationToken:String,deletedAt:{type:Date,index:true}},{timestamps:true});userSchema.pre('save',async function(next){if(this.isModified('password'))this.password=await bcrypt.hash(this.password,12);next();});userSchema.methods.verifyPassword=function(p){return bcrypt.compare(p,this.password)};export const User=mongoose.model('User',userSchema);
